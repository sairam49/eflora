%h2 Services :
.container
  .row
    .col-xs-12
      .table-responsive
        - if @services.any?
          %table.table.table-bordered.table-hover
            %thead
              %tr
                %th Service
                %th Description
                %th Actions
            %tbody
              - @services.each do |s|
                %tr
                  %td= s.service
                  %td= s.description
                  %td
                    = link_to 'Destroy', service_path(s), method: :delete, data: { confirm: 'Are you sure?' } 
                    = link_to 'Edit', services_edit_path(s)
        - else
          .alert.alert-info
            %p There are no services
